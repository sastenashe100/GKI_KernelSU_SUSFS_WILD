name: 'Setup SukiSU-Ultra'
description: 'Integrate SukiSU-Ultra + KPM header fix + version tracking'
inputs:
  ksu_branch:
    description: 'SukiSU branch (empty = 149f65faf)'
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: 'Download and setup SukiSU-Ultra'
      shell: bash
      working-directory: ${{ github.workspace }}/kernel
      run: |
        set -euo pipefail
        BRANCH="${{ inputs.ksu_branch }}"
        [ -z "$BRANCH" ] && BRANCH="149f65faf983d0affd7d80d2016cc3bc81574c79"
        echo "Integrating SukiSU-Ultra ($BRANCH)..."
        curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s "$BRANCH"

    - name: 'Fix KPM headers (konoha-kernel-gki method — proven fix)'
      shell: bash
      working-directory: ${{ github.workspace }}/kernel
      run: |
        REPO="KernelSU"
        MODULES_DIR="."
        # Create symlink: kernel/uapi -> ../uapi (SukiSU keeps UAPI outside kernel/)
        if [ ! -d "$MODULES_DIR/$REPO/kernel/uapi" ] && [ -d "$MODULES_DIR/$REPO/uapi" ]; then
          ln -sfn ../uapi "$MODULES_DIR/$REPO/kernel/uapi"
          echo "Created symlink: kernel/uapi -> ../uapi"
        fi
        # Fix kpm.h include path (from konoha-kernel-gki build.sh)
        KPM_HEADER="$MODULES_DIR/$REPO/kernel/kpm/kpm.h"
        if [ -f "$KPM_HEADER" ] && grep -q '#include "uapi/supercall.h"' "$KPM_HEADER" 2>/dev/null; then
          sed -i 's|#include "uapi/supercall.h"|#include "../../uapi/supercall.h"|' "$KPM_HEADER"
          echo "Patched kpm.h: uapi/supercall.h -> ../../uapi/supercall.h"
        fi
        # Fix ALL nested uapi/ includes in uapi/ directory
        UAPI_DIR="$MODULES_DIR/$REPO/uapi"
        if [ -d "$UAPI_DIR" ]; then
          find "$UAPI_DIR" -name "*.h" -exec sed -i 's|#include "uapi/|#include "|g' {} \;
          echo "Fixed nested uapi/ includes"
        fi
        # Add ALL SukiSU subdirs to include path (fixes compact.c -> policy/allowlist.h etc.)
        KB="$MODULES_DIR/$REPO/kernel/Kbuild"
        echo 'subdir-ccflags-y += -I$(src) -I$(src)/kpm -I$(src)/supercall -I$(src)/policy -I$(src)/core -I$(src)/hook -I$(src)/include -I$(src)/feature -I$(src)/selinux -I$(src)/runtime -I$(src)/sulog -I$(src)/infra -I$(src)/other' >> "$KB"
        echo "Added subdir-ccflags-y (propagates to kpm/)"

    - name: 'Set SukiSU version (needed for App Profile)'
      shell: bash
      working-directory: ${{ github.workspace }}/kernel
      run: |
        cd KernelSU/kernel
        COMMITS_COUNT=$(/usr/bin/git rev-list --count HEAD)
        KSU_VERSION=$(expr $COMMITS_COUNT "+" 30000)
        sed -i "s/^KSU_VERSION_FALLBACK := 1\$/KSU_VERSION_FALLBACK := ${KSU_VERSION}/" Kbuild
        KSU_GIT_TAG="$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.1")"
        sed -i "s/^KSU_VERSION_TAG_FALLBACK := v0.0.1\$/KSU_VERSION_TAG_FALLBACK := ${KSU_GIT_TAG}/" Kbuild
        echo "SukiSU version: $KSU_VERSION"

    - name: 'Enable Kernel Configs'
      uses: ./.github/actions/set-kernel-config
      with:
        config_list: |
          CONFIG_KSU=y
          CONFIG_KPM=y
